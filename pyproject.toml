[project]
authors = [{name = "Vitor Shen", email = "17490173+shenvitor@users.noreply.github.com"}]
dependencies = []
description = "Add a short description here"
name = "gluex-nstar"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
gluex-nstar = {path = ".", editable = true}

[tool.pixi.tasks]
